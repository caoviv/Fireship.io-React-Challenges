function DisplayChallenge({ name }) {
  return (
    <p>Implement a button with onclick = handleClick() function to handle state using useState()which increases a count and a previous count</p>
  );
}

export default DisplayChallenge;
