function DisplayChallenge({ name }) {
  return (
    <p>
      Implement a CountdownTimer component that implements useState() and
      useEffect() in conjunction with setInterval to handle the timer. Make sure
      you use the useEffect() hook to call clearTimeout() when the component is
      destroyed.
    </p>
  );
}

export default DisplayChallenge;
